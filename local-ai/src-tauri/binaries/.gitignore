# Ignore the bundled binaries themselves — they're fetched, not authored.
# Keep the README, this .gitignore, and the llama-cpp/ subdir (with its own .gitignore) tracked.
*
!.gitignore
!README.md
!llama-cpp/
